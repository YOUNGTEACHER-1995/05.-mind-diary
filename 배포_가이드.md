# 📔 마음일기 웹앱 배포 및 설정 가이드

본 가이드는 구글 스프레드시트(GAS 백엔드)와 Vercel(React 프론트엔드)을 설정하고 배포하는 과정을 설명합니다.

---

## 1. Google Apps Script (백엔드) 설정

### 1-1. 스프레드시트 및 스크립트 편집기 열기
1. 구글 드라이브에서 **새 Google 스프레드시트**를 만듭니다. (이름: 마음일기 데이터 등 자유롭게 설정)
2. 상단 메뉴에서 **[확장 프로그램]** > **[Apps Script]**를 클릭하여 스크립트 편집기를 엽니다.
3. 생성해 드린 `backend/Code.gs` 파일의 전체 코드를 복사하여 스크립트 편집기의 `Code.gs` 영역에 덮어쓰기 합니다.

### 1-2. Gemini API 키 발급 및 설정
1. [Google AI Studio](https://aistudio.google.com/)에 접속하여 **Gemini API 키**를 발급받습니다.
2. 스크립트 편집기의 왼쪽 메뉴(톱니바퀴 아이콘) **[프로젝트 설정]**으로 이동합니다.
3. 맨 아래 **[스크립트 속성]** 섹션에서 **[스크립트 속성 추가]**를 클릭합니다.
4. 속성(Key)에 `GEMINI_API_KEY`를 입력하고, 값(Value)에 발급받은 **API 키**를 붙여넣은 뒤 저장합니다.

### 1-3. 웹앱 배포 (Web App Deployment)
1. 스크립트 편집기 우측 상단의 **[배포(Deploy)]** > **[새 배포(New deployment)]**를 클릭합니다.
2. 톱니바퀴 아이콘을 눌러 **[웹앱(Web app)]**을 선택합니다.
3. 설정은 다음과 같이 맞춥니다:
   - 실행 주체 (Execute as): **나 (Me)**
   - 액세스 권한 (Who has access): **모든 사용자 (Anyone)**
4. **[배포]** 버튼을 누르고, 처음엔 권한 승인 창이 뜨면 **[고급] > [(안전하지 않음)으로 이동]**을 통해 권한을 허용해 줍니다.
5. 배포가 완료되면 제공되는 **웹앱 URL (Web app URL)**을 복사합니다.

---

## 2. React 프론트엔드 (Vercel) 배포

### 2-1. 로컬 환경 테스트
1. 폴더 `frontend` 내의 `.env` 파일을 엽니다.
2. `VITE_GAS_URL=여기에_GAS_웹앱_URL을_입력하세요` 부분을 방금 복사한 **웹앱 URL**로 변경하고 저장합니다.
3. 터미널에서 `npm run dev`를 실행하여 브라우저에서 디자인과 기능이 잘 동작하는지 확인합니다.

### 2-2. GitHub 업로드 및 Vercel 배포
1. 현재 `05. mind diary` 폴더(또는 `frontend` 폴더)를 본인의 GitHub 저장소(Repository)에 푸시(Push)합니다.
2. [Vercel](https://vercel.com)에 로그인하고 **[Add New Project]**를 클릭합니다.
3. 푸시한 GitHub 저장소를 선택(Import)합니다.
4. `Root Directory`를 `frontend`로 설정합니다. (또는 최상위 폴더가 frontend라면 그대로 둡니다.)
5. 환경 변수 (Environment Variables) 설정란에 아래와 같이 추가합니다:
   - Name: `VITE_GAS_URL`
   - Value: `복사해 둔 GAS 웹앱 URL`
6. **[Deploy]** 버튼을 눌러 배포를 완료합니다!

🎉 이제 배포된 Vercel 링크를 학생들에게 공유하면, 학생들이 작성한 마음 일기와 Gemini 선생님의 따뜻한 피드백이 시트에 차곡차곡 쌓이게 됩니다.
